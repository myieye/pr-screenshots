# pr-screenshots

Disposable image host for embedding screenshots in GitHub PRs and issues.

Images are pushed here via the GitHub Contents API by the `upload-screenshot` Claude Code skill, which returns a `raw.githubusercontent.com` URL that renders inline in PR/issue markdown. Contents are disposable — prune freely.
